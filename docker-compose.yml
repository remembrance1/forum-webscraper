version: "3.9"

services:
  web:
    build: .
    container_name: forum-webscraper
    ports:
      - "80:8000"          # Pi port 80 -> container port 8000
    environment:
      FLASK_ENV: production
    volumes:
      - app_data:/app/instance
    restart: unless-stopped

volumes:
  app_data:
